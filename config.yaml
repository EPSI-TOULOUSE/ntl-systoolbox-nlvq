# ============================================================
# NTL-SysToolbox - Configuration Nord Transit Logistics
# Surchargeable via variables d'environnement (voir main.py)
# ============================================================

# --- Serveur Windows (Active Directory / DNS) ---
windows:
  ip: "192.168.1.100"
  user: "NTL"
  password: "Azertyuiop!1"
  port: 5985 # WinRM HTTP (5986 pour HTTPS)

# --- Serveur Linux (WMS / MySQL) ---
linux:
  ip: "192.168.1.18"
  user: "quentin"
  password: "quentin"
  port: 22 # SSH
  mysql_port: 3306

# --- Base de données MySQL ---
mysql:
  host: "192.168.1.18"
  port: 3306
  user: "lucas"
  password: "Azertyuiop!1"
  database: "wms_db"
  backup_dir: "backups" # Dossier relatif de sauvegarde

# --- Réseau à scanner (audit obsolescence) ---
network:
  subnet: "192.168.1.0/24"
  ping_timeout: 1 # Timeout en secondes par hôte

# --- Journalisation ---
logging:
  level: "INFO"
  log_dir: "logs"
